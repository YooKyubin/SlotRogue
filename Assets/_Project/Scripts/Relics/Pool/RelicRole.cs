namespace SlotRogue.Relics.Pool
{
    /// <summary>유물의 역할 분류(v20.3). HTML role(dmg/def/hp/st)에 Utility를 더한 집합.</summary>
    public enum RelicRole
    {
        Damage = 0,
        Defense = 1,
        Heal = 2,
        Status = 3,
        Utility = 4,
    }
}
